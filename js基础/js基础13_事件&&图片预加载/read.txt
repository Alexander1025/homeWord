事件（javascript高级程序设计346）
事件流（捕获，事件目标，冒泡）
事件类型（event.type(click,mousedown....)）
事件目标（event.target,event.srcElement）
事件注册（addEventListener(),removeEventListener(),attachEvent(),detachEvent()）(javascript 权威指南 451)
1.设置javascrpt对象属性为事件处理程序
2.设置html标签属性为事件处理程序（<button onclick = "alert('thank you');">点击这里</button>）
3.addEventListener(),(attachEvent()->ie9之前,this===window)

事件冒泡(bubble)、捕获(capture)
取消事件冒泡(cancelBubble = true)，捕获(e.stopPropagation())，取消默认事件(event.preventDefault(),event.returnValue = false , return false);
事件委托